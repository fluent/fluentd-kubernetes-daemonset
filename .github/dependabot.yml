# AUTOMATICALLY GENERATED
# DO NOT EDIT THIS FILE DIRECTLY, USE /templates/dependabot.yml.erb
#
# DISABLE "Dependabot Security Updates" because it scans past
# archived-image directory. It creates noisy PRs. We can't stop that
# behavior at all.
#
version: 2
updates:
  # Maintain dependencies for GitHub Actions
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "daily"
    groups:
      # PR: "Security update [package] from [old] to [new]"
      # This PR should be merged in hurry
      security-updates:
        applies-to: security-updates
        patterns:
          - "*"

      # PR: "Bump [package] from [old] to [new]"
      # No need to be merged this PR in hurry. It is enough to merge
      # once in a month.
      monthly-updates:
        applies-to: version-updates
        patterns:
          - "*"
    # Allow to create PR both of security and normal updates.
    open-pull-requests-limit: 1


  # security updates in daily, ignore normal updates
  - package-ecosystem: "bundler"
    directory: "/docker-image/v1.19/arm64/debian-azureblob"
    schedule:
      interval: "daily"
    groups:
      security-updates:
        applies-to: security-updates
        patterns:
          - "*"
    ignore:
      - dependency-name: "*"
        update-types: ["version-update"]
    # Check [Security and quality] or [Insights] > [Dependency graph]
    # periodically It will help to know security issues in each
    # Gemfile, but no need to create PR automatically.
    open-pull-requests-limit: 0

  # security updates in daily, ignore normal updates
  - package-ecosystem: "bundler"
    directory: "/docker-image/v1.19/arm64/debian-cloudwatch"
    schedule:
      interval: "daily"
    groups:
      security-updates:
        applies-to: security-updates
        patterns:
          - "*"
    ignore:
      - dependency-name: "*"
        update-types: ["version-update"]
    # Check [Security and quality] or [Insights] > [Dependency graph]
    # periodically It will help to know security issues in each
    # Gemfile, but no need to create PR automatically.
    open-pull-requests-limit: 0

  # security updates in daily, ignore normal updates
  - package-ecosystem: "bundler"
    directory: "/docker-image/v1.19/arm64/debian-datadog"
    schedule:
      interval: "daily"
    groups:
      security-updates:
        applies-to: security-updates
        patterns:
          - "*"
    ignore:
      - dependency-name: "*"
        update-types: ["version-update"]
    # Check [Security and quality] or [Insights] > [Dependency graph]
    # periodically It will help to know security issues in each
    # Gemfile, but no need to create PR automatically.
    open-pull-requests-limit: 0

  # security updates in daily, ignore normal updates
  - package-ecosystem: "bundler"
    directory: "/docker-image/v1.19/arm64/debian-elasticsearch7"
    schedule:
      interval: "daily"
    groups:
      security-updates:
        applies-to: security-updates
        patterns:
          - "*"
    ignore:
      - dependency-name: "*"
        update-types: ["version-update"]
    # Check [Security and quality] or [Insights] > [Dependency graph]
    # periodically It will help to know security issues in each
    # Gemfile, but no need to create PR automatically.
    open-pull-requests-limit: 0

  # security updates in daily, ignore normal updates
  - package-ecosystem: "bundler"
    directory: "/docker-image/v1.19/arm64/debian-elasticsearch8"
    schedule:
      interval: "daily"
    groups:
      security-updates:
        applies-to: security-updates
        patterns:
          - "*"
    ignore:
      - dependency-name: "*"
        update-types: ["version-update"]
    # Check [Security and quality] or [Insights] > [Dependency graph]
    # periodically It will help to know security issues in each
    # Gemfile, but no need to create PR automatically.
    open-pull-requests-limit: 0

  # security updates in daily, ignore normal updates
  - package-ecosystem: "bundler"
    directory: "/docker-image/v1.19/arm64/debian-forward"
    schedule:
      interval: "daily"
    groups:
      security-updates:
        applies-to: security-updates
        patterns:
          - "*"
    ignore:
      - dependency-name: "*"
        update-types: ["version-update"]
    # Check [Security and quality] or [Insights] > [Dependency graph]
    # periodically It will help to know security issues in each
    # Gemfile, but no need to create PR automatically.
    open-pull-requests-limit: 0

  # security updates in daily, ignore normal updates
  - package-ecosystem: "bundler"
    directory: "/docker-image/v1.19/arm64/debian-gcs"
    schedule:
      interval: "daily"
    groups:
      security-updates:
        applies-to: security-updates
        patterns:
          - "*"
    ignore:
      - dependency-name: "*"
        update-types: ["version-update"]
    # Check [Security and quality] or [Insights] > [Dependency graph]
    # periodically It will help to know security issues in each
    # Gemfile, but no need to create PR automatically.
    open-pull-requests-limit: 0

  # security updates in daily, ignore normal updates
  - package-ecosystem: "bundler"
    directory: "/docker-image/v1.19/arm64/debian-graylog"
    schedule:
      interval: "daily"
    groups:
      security-updates:
        applies-to: security-updates
        patterns:
          - "*"
    ignore:
      - dependency-name: "*"
        update-types: ["version-update"]
    # Check [Security and quality] or [Insights] > [Dependency graph]
    # periodically It will help to know security issues in each
    # Gemfile, but no need to create PR automatically.
    open-pull-requests-limit: 0

  # security updates in daily, ignore normal updates
  - package-ecosystem: "bundler"
    directory: "/docker-image/v1.19/arm64/debian-kafka"
    schedule:
      interval: "daily"
    groups:
      security-updates:
        applies-to: security-updates
        patterns:
          - "*"
    ignore:
      - dependency-name: "*"
        update-types: ["version-update"]
    # Check [Security and quality] or [Insights] > [Dependency graph]
    # periodically It will help to know security issues in each
    # Gemfile, but no need to create PR automatically.
    open-pull-requests-limit: 0

  # security updates in daily, ignore normal updates
  - package-ecosystem: "bundler"
    directory: "/docker-image/v1.19/arm64/debian-kafka2"
    schedule:
      interval: "daily"
    groups:
      security-updates:
        applies-to: security-updates
        patterns:
          - "*"
    ignore:
      - dependency-name: "*"
        update-types: ["version-update"]
    # Check [Security and quality] or [Insights] > [Dependency graph]
    # periodically It will help to know security issues in each
    # Gemfile, but no need to create PR automatically.
    open-pull-requests-limit: 0

  # security updates in daily, ignore normal updates
  - package-ecosystem: "bundler"
    directory: "/docker-image/v1.19/arm64/debian-kinesis"
    schedule:
      interval: "daily"
    groups:
      security-updates:
        applies-to: security-updates
        patterns:
          - "*"
    ignore:
      - dependency-name: "*"
        update-types: ["version-update"]
    # Check [Security and quality] or [Insights] > [Dependency graph]
    # periodically It will help to know security issues in each
    # Gemfile, but no need to create PR automatically.
    open-pull-requests-limit: 0

  # security updates in daily, ignore normal updates
  - package-ecosystem: "bundler"
    directory: "/docker-image/v1.19/arm64/debian-logentries"
    schedule:
      interval: "daily"
    groups:
      security-updates:
        applies-to: security-updates
        patterns:
          - "*"
    ignore:
      - dependency-name: "*"
        update-types: ["version-update"]
    # Check [Security and quality] or [Insights] > [Dependency graph]
    # periodically It will help to know security issues in each
    # Gemfile, but no need to create PR automatically.
    open-pull-requests-limit: 0

  # security updates in daily, ignore normal updates
  - package-ecosystem: "bundler"
    directory: "/docker-image/v1.19/arm64/debian-loggly"
    schedule:
      interval: "daily"
    groups:
      security-updates:
        applies-to: security-updates
        patterns:
          - "*"
    ignore:
      - dependency-name: "*"
        update-types: ["version-update"]
    # Check [Security and quality] or [Insights] > [Dependency graph]
    # periodically It will help to know security issues in each
    # Gemfile, but no need to create PR automatically.
    open-pull-requests-limit: 0

  # security updates in daily, ignore normal updates
  - package-ecosystem: "bundler"
    directory: "/docker-image/v1.19/arm64/debian-logzio"
    schedule:
      interval: "daily"
    groups:
      security-updates:
        applies-to: security-updates
        patterns:
          - "*"
    ignore:
      - dependency-name: "*"
        update-types: ["version-update"]
    # Check [Security and quality] or [Insights] > [Dependency graph]
    # periodically It will help to know security issues in each
    # Gemfile, but no need to create PR automatically.
    open-pull-requests-limit: 0

  # security updates in daily, ignore normal updates
  - package-ecosystem: "bundler"
    directory: "/docker-image/v1.19/arm64/debian-opensearch"
    schedule:
      interval: "daily"
    groups:
      security-updates:
        applies-to: security-updates
        patterns:
          - "*"
    ignore:
      - dependency-name: "*"
        update-types: ["version-update"]
    # Check [Security and quality] or [Insights] > [Dependency graph]
    # periodically It will help to know security issues in each
    # Gemfile, but no need to create PR automatically.
    open-pull-requests-limit: 0

  # security updates in daily, ignore normal updates
  - package-ecosystem: "bundler"
    directory: "/docker-image/v1.19/arm64/debian-papertrail"
    schedule:
      interval: "daily"
    groups:
      security-updates:
        applies-to: security-updates
        patterns:
          - "*"
    ignore:
      - dependency-name: "*"
        update-types: ["version-update"]
    # Check [Security and quality] or [Insights] > [Dependency graph]
    # periodically It will help to know security issues in each
    # Gemfile, but no need to create PR automatically.
    open-pull-requests-limit: 0

  # security updates in daily, ignore normal updates
  - package-ecosystem: "bundler"
    directory: "/docker-image/v1.19/arm64/debian-s3"
    schedule:
      interval: "daily"
    groups:
      security-updates:
        applies-to: security-updates
        patterns:
          - "*"
    ignore:
      - dependency-name: "*"
        update-types: ["version-update"]
    # Check [Security and quality] or [Insights] > [Dependency graph]
    # periodically It will help to know security issues in each
    # Gemfile, but no need to create PR automatically.
    open-pull-requests-limit: 0

  # security updates in daily, ignore normal updates
  - package-ecosystem: "bundler"
    directory: "/docker-image/v1.19/arm64/debian-syslog"
    schedule:
      interval: "daily"
    groups:
      security-updates:
        applies-to: security-updates
        patterns:
          - "*"
    ignore:
      - dependency-name: "*"
        update-types: ["version-update"]
    # Check [Security and quality] or [Insights] > [Dependency graph]
    # periodically It will help to know security issues in each
    # Gemfile, but no need to create PR automatically.
    open-pull-requests-limit: 0

  # security updates in daily, ignore normal updates
  - package-ecosystem: "bundler"
    directory: "/docker-image/v1.19/debian-azureblob"
    schedule:
      interval: "daily"
    groups:
      security-updates:
        applies-to: security-updates
        patterns:
          - "*"
    ignore:
      - dependency-name: "*"
        update-types: ["version-update"]
    # Check [Security and quality] or [Insights] > [Dependency graph]
    # periodically It will help to know security issues in each
    # Gemfile, but no need to create PR automatically.
    open-pull-requests-limit: 0

  # security updates in daily, ignore normal updates
  - package-ecosystem: "bundler"
    directory: "/docker-image/v1.19/debian-cloudwatch"
    schedule:
      interval: "daily"
    groups:
      security-updates:
        applies-to: security-updates
        patterns:
          - "*"
    ignore:
      - dependency-name: "*"
        update-types: ["version-update"]
    # Check [Security and quality] or [Insights] > [Dependency graph]
    # periodically It will help to know security issues in each
    # Gemfile, but no need to create PR automatically.
    open-pull-requests-limit: 0

  # security updates in daily, ignore normal updates
  - package-ecosystem: "bundler"
    directory: "/docker-image/v1.19/debian-datadog"
    schedule:
      interval: "daily"
    groups:
      security-updates:
        applies-to: security-updates
        patterns:
          - "*"
    ignore:
      - dependency-name: "*"
        update-types: ["version-update"]
    # Check [Security and quality] or [Insights] > [Dependency graph]
    # periodically It will help to know security issues in each
    # Gemfile, but no need to create PR automatically.
    open-pull-requests-limit: 0

  # security updates in daily, ignore normal updates
  - package-ecosystem: "bundler"
    directory: "/docker-image/v1.19/debian-elasticsearch7"
    schedule:
      interval: "daily"
    groups:
      security-updates:
        applies-to: security-updates
        patterns:
          - "*"
    ignore:
      - dependency-name: "*"
        update-types: ["version-update"]
    # Check [Security and quality] or [Insights] > [Dependency graph]
    # periodically It will help to know security issues in each
    # Gemfile, but no need to create PR automatically.
    open-pull-requests-limit: 0

  # security updates in daily, ignore normal updates
  - package-ecosystem: "bundler"
    directory: "/docker-image/v1.19/debian-elasticsearch8"
    schedule:
      interval: "daily"
    groups:
      security-updates:
        applies-to: security-updates
        patterns:
          - "*"
    ignore:
      - dependency-name: "*"
        update-types: ["version-update"]
    # Check [Security and quality] or [Insights] > [Dependency graph]
    # periodically It will help to know security issues in each
    # Gemfile, but no need to create PR automatically.
    open-pull-requests-limit: 0

  # security updates in daily, ignore normal updates
  - package-ecosystem: "bundler"
    directory: "/docker-image/v1.19/debian-forward"
    schedule:
      interval: "daily"
    groups:
      security-updates:
        applies-to: security-updates
        patterns:
          - "*"
    ignore:
      - dependency-name: "*"
        update-types: ["version-update"]
    # Check [Security and quality] or [Insights] > [Dependency graph]
    # periodically It will help to know security issues in each
    # Gemfile, but no need to create PR automatically.
    open-pull-requests-limit: 0

  # security updates in daily, ignore normal updates
  - package-ecosystem: "bundler"
    directory: "/docker-image/v1.19/debian-gcs"
    schedule:
      interval: "daily"
    groups:
      security-updates:
        applies-to: security-updates
        patterns:
          - "*"
    ignore:
      - dependency-name: "*"
        update-types: ["version-update"]
    # Check [Security and quality] or [Insights] > [Dependency graph]
    # periodically It will help to know security issues in each
    # Gemfile, but no need to create PR automatically.
    open-pull-requests-limit: 0

  # security updates in daily, ignore normal updates
  - package-ecosystem: "bundler"
    directory: "/docker-image/v1.19/debian-graylog"
    schedule:
      interval: "daily"
    groups:
      security-updates:
        applies-to: security-updates
        patterns:
          - "*"
    ignore:
      - dependency-name: "*"
        update-types: ["version-update"]
    # Check [Security and quality] or [Insights] > [Dependency graph]
    # periodically It will help to know security issues in each
    # Gemfile, but no need to create PR automatically.
    open-pull-requests-limit: 0

  # security updates in daily, ignore normal updates
  - package-ecosystem: "bundler"
    directory: "/docker-image/v1.19/debian-kafka"
    schedule:
      interval: "daily"
    groups:
      security-updates:
        applies-to: security-updates
        patterns:
          - "*"
    ignore:
      - dependency-name: "*"
        update-types: ["version-update"]
    # Check [Security and quality] or [Insights] > [Dependency graph]
    # periodically It will help to know security issues in each
    # Gemfile, but no need to create PR automatically.
    open-pull-requests-limit: 0

  # security updates in daily, ignore normal updates
  - package-ecosystem: "bundler"
    directory: "/docker-image/v1.19/debian-kafka2"
    schedule:
      interval: "daily"
    groups:
      security-updates:
        applies-to: security-updates
        patterns:
          - "*"
    ignore:
      - dependency-name: "*"
        update-types: ["version-update"]
    # Check [Security and quality] or [Insights] > [Dependency graph]
    # periodically It will help to know security issues in each
    # Gemfile, but no need to create PR automatically.
    open-pull-requests-limit: 0

  # security updates in daily, ignore normal updates
  - package-ecosystem: "bundler"
    directory: "/docker-image/v1.19/debian-kinesis"
    schedule:
      interval: "daily"
    groups:
      security-updates:
        applies-to: security-updates
        patterns:
          - "*"
    ignore:
      - dependency-name: "*"
        update-types: ["version-update"]
    # Check [Security and quality] or [Insights] > [Dependency graph]
    # periodically It will help to know security issues in each
    # Gemfile, but no need to create PR automatically.
    open-pull-requests-limit: 0

  # security updates in daily, ignore normal updates
  - package-ecosystem: "bundler"
    directory: "/docker-image/v1.19/debian-logentries"
    schedule:
      interval: "daily"
    groups:
      security-updates:
        applies-to: security-updates
        patterns:
          - "*"
    ignore:
      - dependency-name: "*"
        update-types: ["version-update"]
    # Check [Security and quality] or [Insights] > [Dependency graph]
    # periodically It will help to know security issues in each
    # Gemfile, but no need to create PR automatically.
    open-pull-requests-limit: 0

  # security updates in daily, ignore normal updates
  - package-ecosystem: "bundler"
    directory: "/docker-image/v1.19/debian-loggly"
    schedule:
      interval: "daily"
    groups:
      security-updates:
        applies-to: security-updates
        patterns:
          - "*"
    ignore:
      - dependency-name: "*"
        update-types: ["version-update"]
    # Check [Security and quality] or [Insights] > [Dependency graph]
    # periodically It will help to know security issues in each
    # Gemfile, but no need to create PR automatically.
    open-pull-requests-limit: 0

  # security updates in daily, ignore normal updates
  - package-ecosystem: "bundler"
    directory: "/docker-image/v1.19/debian-logzio"
    schedule:
      interval: "daily"
    groups:
      security-updates:
        applies-to: security-updates
        patterns:
          - "*"
    ignore:
      - dependency-name: "*"
        update-types: ["version-update"]
    # Check [Security and quality] or [Insights] > [Dependency graph]
    # periodically It will help to know security issues in each
    # Gemfile, but no need to create PR automatically.
    open-pull-requests-limit: 0

  # security updates in daily, ignore normal updates
  - package-ecosystem: "bundler"
    directory: "/docker-image/v1.19/debian-opensearch"
    schedule:
      interval: "daily"
    groups:
      security-updates:
        applies-to: security-updates
        patterns:
          - "*"
    ignore:
      - dependency-name: "*"
        update-types: ["version-update"]
    # Check [Security and quality] or [Insights] > [Dependency graph]
    # periodically It will help to know security issues in each
    # Gemfile, but no need to create PR automatically.
    open-pull-requests-limit: 0

  # security updates in daily, ignore normal updates
  - package-ecosystem: "bundler"
    directory: "/docker-image/v1.19/debian-papertrail"
    schedule:
      interval: "daily"
    groups:
      security-updates:
        applies-to: security-updates
        patterns:
          - "*"
    ignore:
      - dependency-name: "*"
        update-types: ["version-update"]
    # Check [Security and quality] or [Insights] > [Dependency graph]
    # periodically It will help to know security issues in each
    # Gemfile, but no need to create PR automatically.
    open-pull-requests-limit: 0

  # security updates in daily, ignore normal updates
  - package-ecosystem: "bundler"
    directory: "/docker-image/v1.19/debian-s3"
    schedule:
      interval: "daily"
    groups:
      security-updates:
        applies-to: security-updates
        patterns:
          - "*"
    ignore:
      - dependency-name: "*"
        update-types: ["version-update"]
    # Check [Security and quality] or [Insights] > [Dependency graph]
    # periodically It will help to know security issues in each
    # Gemfile, but no need to create PR automatically.
    open-pull-requests-limit: 0

  # security updates in daily, ignore normal updates
  - package-ecosystem: "bundler"
    directory: "/docker-image/v1.19/debian-syslog"
    schedule:
      interval: "daily"
    groups:
      security-updates:
        applies-to: security-updates
        patterns:
          - "*"
    ignore:
      - dependency-name: "*"
        update-types: ["version-update"]
    # Check [Security and quality] or [Insights] > [Dependency graph]
    # periodically It will help to know security issues in each
    # Gemfile, but no need to create PR automatically.
    open-pull-requests-limit: 0
