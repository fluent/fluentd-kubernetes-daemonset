
# AUTOMATICALLY GENERATED
# DO NOT EDIT THIS FILE DIRECTLY, USE /templates/conf/fluent.conf.erb

@include systemd.conf
@include kubernetes.conf

<match **>
   @type secure_forward
   shared_key "#{ENV['FLUENT_SECURE_FORWARD_SHARED_KEY']}"
   self_hostname client.fqdn.local
   secure true
   ca_cert_path "#{ENV['FLUENT_SECURE_FORWARD_CERT_PATH']}"
   config_path /etc/logentries/tokens.yaml
  <server>
    host "#{ENV['FLUENT_SECURE_FORWARD_REMOTE_HOST']}"
    port "#{ENV['FLUENT_SECURE_FORWARD_PORT'] || 24284 }"
  </server>

</match>

